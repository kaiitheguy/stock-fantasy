diff --git a/node_modules/react-native-swipe-cards-deck/SwipeCards.js b/node_modules/react-native-swipe-cards-deck/SwipeCards.js
index 4fcbd54..f77dd4c 100644
--- a/node_modules/react-native-swipe-cards-deck/SwipeCards.js
+++ b/node_modules/react-native-swipe-cards-deck/SwipeCards.js
@@ -2,7 +2,7 @@
 
 import React, { Component } from "react";
 import PropTypes from "prop-types";
-import { View, Animated, PanResponder, ViewPropTypes } from "react-native";
+import { View, Animated, PanResponder } from "react-native";
 import Defaults from "./Defaults";
 import clamp from "clamp";
 import { styles } from "./Styles";
@@ -10,6 +10,14 @@ let currentIndex = {};
 //Components could be unloaded and loaded and we will loose the users currentIndex, we can persist it here.
 let currentIndex = {};
 let guid = 0;
+
+const ViewStyleProp = PropTypes.oneOfType([
+  PropTypes.array,
+  PropTypes.object,
+  PropTypes.number,
+]);
+
+const ViewPropTypes = { style: ViewStyleProp };
 
 const actionShape = PropTypes.shape({
   show: PropTypes.bool,